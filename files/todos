Demo:
    #1. param testing --> Done
    #2. move dev code into dev branch from main before cleaning --> done
    #3. remove prints, comments, old code --> done
    #4. provide documentation
    #5. readme.md
    #6. demo gif
    #7. provide instructions

FUTURE:
    # Arguments
        #---------------------------------------------------------------- visual -----------------------------------------------------------------#
        # display_width=1024                                    # broken
        # display_height=${buffer_size}                         # broken
        #---------------------------------------------------------------- camera -----------------------------------------------------------------#
        camera_resolution_widt=640                            # checks for when cam_ > disp_
        camera_resolution_height=480                           # checks for when cam_ > disp_
        roi_radius=100 #$( bc -l <<<"0.1*$display_height" )    # do not work for large rois

    # audio class:
        # get all input devices
        # mix multiple signal sources into output signal

    # fix mirroring camera frame

    # comply to the rule of 5 in class designing

    # Makefile -- upgrade compiling



